server:
  port: 8080

spring:
  application:
    name: ec-system
  datasource:
    url: jdbc:mysql://localhost:3306/ec_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  sql:
    init:
      mode: never

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

app:
  settlement:
    cron: 0 0 2 * * *
  rocketmq:
    enabled: false
    order-completed-topic: order-completed-topic

rocketmq:
  name-server: localhost:9876
  producer:
    group: ec-system-producer-group

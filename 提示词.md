# 角色：资深Java开发专家

## 任务
请帮我生成一个完整的Spring Boot电商系统代码，实现预存账户支付功能。

## 技术栈要求
- **Java 21** (使用最新的LTS版本特性)
- Spring Boot 3.2.x (完全兼容Java 21)
- Spring Cloud 2023.0.x (OpenFeign for 服务间调用，预留微服务架构)
- **MyBatis-Plus 3.5.5** (支持Java 21)
- MySQL 8.0
- Spring Data Redis (用于库存缓存、分布式锁)
- RocketMQ 5.1.0 (使用Java 21兼容版本)
- Maven 构建工具
- H2 (单元测试用)

## 功能需求

### 1. 用户模块
- 用户注册（简化版，无需鉴权）
- 预存账户充值API: `POST /api/users/{userId}/accounts/recharge`
- 查询余额API: `GET /api/users/{userId}/accounts/balance`

### 2. 商家模块  
- 商家注册
- 添加商品库存API: `POST /api/merchants/{merchantId}/inventory`
- 查询商家账户余额API: `GET /api/merchants/{merchantId}/accounts/balance`
- 定时结算Job: 每天凌晨2点执行，核对售出商品总价值与商家账户余额

### 3. 商品购买流程（核心）

用户下单购买商品:
POST /api/orders
Request: {
"userId": 1,
"merchantId": 1,
"items": [{"sku": "SKU001", "quantity": 2}]
}

流程：

1. 开启本地事务 + Redis分布式锁（用户级、商品级）
2. 验证商品库存（查询MySQL，Redis缓存）
3. 计算总金额 = sum(quantity * price)
4. 发送RocketMQ预扣款消息（可选）
5. 用户账户扣款（需检查余额，使用乐观锁）
6. 商家账户加款（使用乐观锁）
7. 扣减库存（使用乐观锁，MyBatis-Plus的@Version）
8. 创建订单记录（状态：已完成）
9. 提交事务
10. 发送订单完成消息到RocketMQ

text

```
### 4. 数据库设计

​```sql
-- 用户表
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 商家表  
CREATE TABLE merchants (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    balance DECIMAL(15,2) DEFAULT 0.00,
    version INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 用户账户表（预存账户）
CREATE TABLE user_accounts (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT UNIQUE NOT NULL,
    balance DECIMAL(15,2) DEFAULT 0.00,
    version INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 商品表（商家库存）
CREATE TABLE products (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    merchant_id BIGINT NOT NULL,
    sku VARCHAR(50) NOT NULL,
    name VARCHAR(200),
    price DECIMAL(10,2) NOT NULL,
    quantity INT NOT NULL,
    version INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_merchant_sku (merchant_id, sku),
    FOREIGN KEY (merchant_id) REFERENCES merchants(id)
);

-- 订单表
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    merchant_id BIGINT NOT NULL,
    order_no VARCHAR(32) UNIQUE NOT NULL,
    total_amount DECIMAL(15,2) NOT NULL,
    status VARCHAR(20) DEFAULT 'COMPLETED',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user (user_id),
    INDEX idx_merchant (merchant_id)
);

-- 订单明细表
CREATE TABLE order_items (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id BIGINT NOT NULL,
    product_id BIGINT NOT NULL,
    sku VARCHAR(50),
    quantity INT NOT NULL,
    price DECIMAL(10,2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (order_id) REFERENCES orders(id)
);

-- 结算记录表
CREATE TABLE settlement_records (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    merchant_id BIGINT NOT NULL,
    settlement_date DATE NOT NULL,
    total_sales_amount DECIMAL(15,2),
    merchant_balance DECIMAL(15,2),
    is_matched BOOLEAN,
    difference DECIMAL(15,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_merchant_date (merchant_id, settlement_date)
);
```



## API设计规范（无鉴权）



text

```
POST   /api/v1/users                     - 注册用户
       Request: {"name": "张三"}
       Response: {"id": 1, "name": "张三"}

GET    /api/v1/users/{userId}/balance    - 查询余额
       Response: {"userId": 1, "balance": 1000.00}

POST   /api/v1/users/{userId}/recharge   - 充值
       Request: {"amount": 100.00}
       Response: {"userId": 1, "balance": 1100.00}
```



### 商家端API

text

```
POST   /api/v1/merchants                          - 注册商家
       Request: {"name": "王掌柜商店"}
       Response: {"id": 1, "name": "王掌柜商店", "balance": 0.00}

GET    /api/v1/merchants/{merchantId}/balance     - 查询账户余额
       Response: {"merchantId": 1, "balance": 5000.00}

POST   /api/v1/merchants/{merchantId}/inventory   - 添加库存
       Request: {
         "sku": "SKU001",
         "name": "商品名称", 
         "price": 99.99,
         "quantity": 100
       }
       Response: {"productId": 1, "sku": "SKU001", "quantity": 100}
```



### 订单API

text

```
POST   /api/v1/orders                             - 创建订单
       Request: {
         "userId": 1,
         "merchantId": 1,
         "items": [
           {"sku": "SKU001", "quantity": 2}
         ]
       }
       Response: {
         "orderId": 1,
         "orderNo": "ORD202601020001",
         "totalAmount": 199.98,
         "status": "COMPLETED"
       }

GET    /api/v1/orders/{orderId}                   - 查询订单
GET    /api/v1/users/{userId}/orders              - 查询用户订单
GET    /api/v1/merchants/{merchantId}/orders      - 查询商家订单
```



## 关键代码结构要求（Java 21风格）

text

```
src/main/java/com/example/ecsystem/
├── controller/
│   ├── UserController.java
│   ├── MerchantController.java  
│   ├── OrderController.java
│   └── SettlementController.java
├── service/
│   ├── UserService.java
│   ├── MerchantService.java
│   ├── OrderService.java (核心业务)
│   ├── AccountService.java
│   ├── InventoryService.java
│   └── SettlementService.java
├── service/impl/
│   ├── UserServiceImpl.java
│   ├── MerchantServiceImpl.java
│   ├── OrderServiceImpl.java (核心业务)
│   ├── AccountServiceImpl.java
│   ├── InventoryServiceImpl.java
│   └── SettlementServiceImpl.java
├── mapper/ (MyBatis-Plus Mapper接口)
│   ├── UserMapper.java
│   ├── MerchantMapper.java
│   ├── UserAccountMapper.java
│   ├── ProductMapper.java
│   ├── OrderMapper.java
│   ├── OrderItemMapper.java
│   └── SettlementRecordMapper.java
├── entity/ (MyBatis-Plus实体类)
│   ├── User.java
│   ├── Merchant.java
│   ├── UserAccount.java
│   ├── Product.java
│   ├── Order.java
│   ├── OrderItem.java
│   └── SettlementRecord.java
├── dto/ (使用Java Record)
│   ├── request/
│   │   ├── UserRegisterRequest.java (record)
│   │   ├── RechargeRequest.java (record)
│   │   ├── MerchantRegisterRequest.java (record)
│   │   ├── AddInventoryRequest.java (record)
│   │   └── CreateOrderRequest.java (record)
│   └── response/
│       ├── UserBalanceResponse.java (record)
│       ├── MerchantBalanceResponse.java (record)
│       ├── OrderResponse.java (record)
│       └── ApiResponse.java (record)
├── config/
│   ├── MybatisPlusConfig.java (分页插件配置)
│   ├── RedisConfig.java
│   ├── RocketMQConfig.java
│   ├── VirtualThreadConfig.java (虚拟线程配置，可选)
│   └── SchedulingConfig.java
├── component/
│   ├── OrderMessageConsumer.java (RocketMQ)
│   └── SettlementJob.java (定时任务)
├── exception/
│   ├── BusinessException.java
│   ├── GlobalExceptionHandler.java
│   └── ErrorCode.java (使用enum)
├── interceptor/
│   └── OptimisticLockerInterceptor.java (乐观锁配置)
├── aspect/
│   └── DistributedLockAspect.java (Redis分布式锁)
└── util/
    ├── RedisLockUtil.java
    └── OrderNoGenerator.java
```